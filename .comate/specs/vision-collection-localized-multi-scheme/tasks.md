# 采集辅助规划「具体地点 + 定位 + 多方案」增强任务计划

- [x] Task 1: 打通高德 POI 真实数据解析（根因修复）
    - 1.1: 新增 `unwrapMcpText`，兼容 `{"content":[{"text":"..."}]}` 包裹与直接 JSON 两种返回
    - 1.2: 增强 `locatePoiArray`，兼容内层文本解析后的 `pois`/`results`/`data.pois`
    - 1.3: 增强 `textOf`，将高德空数组 `[]` 字段视为空，跳过无名记录
    - 1.4: 解析高德 `location`（"lng,lat"）写入候选点经纬度
    - 1.5: 优化 `buildSearchKeywords`，用「位置 + 场景词」组合查询提升命中

- [x] Task 2: 扩展数据模型承载定位与多方案
    - 2.1: `PoiCandidate` 新增可空 `longitude`/`latitude` 字段
    - 2.2: `CollectionSite` 新增 `longitude`/`latitude`/`mapImageUrl`/`travelDistance`/`travelDuration`/`navUrl`，紧凑构造器归一 null
    - 2.3: 新增 `CollectionScheme` 模型（点位组合 + 路线 + 推荐理由 + 距离/耗时/出行方式）
    - 2.4: `CollectionPlan` 新增 `schemes` 字段，`sites` 保留指向首选方案以兼容报告与历史数据
    - 2.5: 更新所有受影响构造点，保持现有评分逻辑不变

- [x] Task 3: 高德路线规划与静态地图接入
    - 3.1: 新增 `enrichLocation`，生成标注点位经纬度的高德静态地图 URL
    - 3.2: 新增 `planRoute`，按出行方式调用高德路线规划工具，解析距离与时长
    - 3.3: 新增 `buildNavUrl`，生成高德导航跳转链接
    - 3.4: 将距离/耗时/地图/导航写入 `CollectionSite`，整体距离/耗时写入方案
    - 3.5: 所有调用失败时对应字段为空，不中断主流程

- [x] Task 4: 多方案生成与组合
    - 4.1: 新增方案组合逻辑（`SchemeComposer` 或 service 内），从真实 POI 池组合 2-3 套
    - 4.2: 方案A 质量优先、方案B 多样性优先、方案C 效率优先三种策略
    - 4.3: 每套方案生成推荐理由
    - 4.4: 每套方案调用路线规划得到全程距离/耗时
    - 4.5: POI 不足时降级为 1 套，保持不报错

- [x] Task 5: 主流程接入
    - 5.1: `CollectionPlanService.plan()` 串联 POI → 评分 → 多方案组合 → 路线/地图/图片增强
    - 5.2: 首选方案点位回填 `CollectionPlan.sites` 保持兼容
    - 5.3: 保持 `recordStore.savePlan`、项目归档行为不变

- [x] Task 6: 后端单元测试与降级回归测试
    - 6.1: 覆盖 `unwrapMcpText` 从 content 包裹提取 POI
    - 6.2: 覆盖高德空数组字段跳过与经纬度解析
    - 6.3: 覆盖多方案组合在充足 POI 下策略不同
    - 6.4: 覆盖 POI 不足降级单套
    - 6.5: 覆盖路线/静态地图失败时字段为空且方案仍生成
    - 6.6: 覆盖历史无新字段记录反序列化不报错

- [x] Task 7: 前端多方案与定位展示 + 贴边修复
    - 7.1: 修复结果区/容器两侧贴边，增加左右边距并自适应窄屏
    - 7.2: 结果区顶部增加方案切换（含标题、推荐理由、全程距离/耗时/出行方式）
    - 7.3: 点位卡片展示高德地图缩略图 + 场景参考图 + 距离/耗时/出行方式 + 导航链接
    - 7.4: 支持多套方案切换查看与多选对比
    - 7.5: 图片/地图加载失败优雅降级，不阻断信息

- [x] Task 8: 运行验证并生成总结
    - 8.1: 运行后端定向测试，修复失败项
    - 8.2: 运行后端全量测试确认无回归
    - 8.3: 运行前端构建
    - 8.4: 执行规格符合性审查、代码质量审查和最终 QA 评估
    - 8.5: 生成 `summary.md`，记录根因修复、定位/多方案效果、降级策略与验证结果
