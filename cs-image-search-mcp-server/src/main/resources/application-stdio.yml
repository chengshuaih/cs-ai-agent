spring:
  ai:
    mcp:
      server:
        name: cs-image-search-mcp-server
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true
  # stdio
  main:
    web-application-type: none
    banner-mode: off

logging:
  level:
    root: off
    com.alibaba.nacos: off
    org.springframework: off
